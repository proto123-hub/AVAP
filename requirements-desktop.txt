# AVAP - desktop preflight dependencies. ASCII only (see requirements.txt).
#
# `python -m avap.preflight pick` opens a window, which the headless OpenCV in
# requirements.txt cannot do (its HighGUI is compiled out). Install this on the
# PC where you pick anchor boxes.
#
# opencv-python and opencv-python-headless share the cv2 import and must not
# coexist. Keep this entry point separate from the headless requirements file.
-r requirements-common.txt
opencv-python>=4.10,<6
