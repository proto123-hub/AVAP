# AVAP - headless CI dependencies (core + tests).
# ASCII only in this file: on a clean Windows venv the bundled pip may read
# this file with the locale codec (CP949) and crash on non-ASCII comments.
# CI uses this headless entry point. Desktop pick uses requirements-desktop.txt.
-r requirements-common.txt
opencv-python-headless>=4.10,<6
